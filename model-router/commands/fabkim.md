---
description: Run the FabKim flow - Claude orchestrates, the routed Kimi worker model builds
---

The brief: $ARGUMENTS

Read this plugin's fabkim skill (`skills/fabkim/SKILL.md`) and follow it end
to end with the brief above. FabKim is the Claude + Kimi split: you (the
session Claude model) plan, spec, and verify; the routed Kimi worker does
100% of the building through the local proxy.
