---
description: Run the FabSol flow - your main model orchestrates, the routed worker model builds
---

The brief: $ARGUMENTS

Read this plugin's fabsol skill (`skills/fabsol/SKILL.md`) and follow it end
to end with the brief above. FabSol is the flow that combines the two models:
you (the session model) plan, spec, and verify; the routed worker model does
100% of the building through the local proxy.
