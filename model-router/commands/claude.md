---
description: Run a Claude-only session workflow on a selected claude-* model using the direct Claude login (not the proxy)
---

The brief: $ARGUMENTS

Read this plugin's claude skill (`skills/claude/SKILL.md`) and follow it
end to end with the brief above. Claude is a full session on a Claude model
(default `claude-fable-5`) with the normal Claude login — not FabSol/FabKim
orchestrator + worker, and not Moonshot (Kimi-only via the proxy).
