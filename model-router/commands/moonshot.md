---
description: Run a Kimi Code / Moonshot session workflow on a selected kimi-* model via the local proxy
---

The brief: $ARGUMENTS

Read this plugin's moonshot skill (`skills/moonshot/SKILL.md`) and follow it
end to end with the brief above. Moonshot is a full session on a Kimi model
(default `kimi-k3`) through CLIProxyAPI — use it for Kimi Code work, not the
FabSol orchestrator + worker split.
